# V0.4 Roadmap

## Build Blockers

- [x] Resolve B0505S Y-cap false short — custom `B0505S_NoYcap_driver` in `lib/b0505s_no_ycap.ato` omits the 270 pF anti-EMI Y-cap that bridges host/isolated GND, eliminating false DRC short detection
- [ ] Replace RP2040 placeholder with full `Raspberry_Pi_RP2040` module (blocked by atopile `has_single_electric_reference_shared` trait in isolated power domains — upstream issue)

## Component Quality

- [x] Replace auto-generated KiCad footprints/symbols with JLCPCB-sourced ones (ADS1256 C28186, ISO7741 C179463, SN65HVD230 C12084) — already present in `parts/`
- [x] Replace ferrite bead resistor placeholders with real Murata BLM15AG601SN1D (LCSC C76884) `Inductor` instances in `galvanic_isolator.ato` and `connectivity.ato`
- [x] Replace ISO7741 placeholder in `galvanic_isolator.ato` and `spi_isolator.ato` with real `ISO7741_driver` from `lib/iso7741.ato` — unblocked by B0505S Y-cap fix

## Architectural

- [x] Add per-channel SPI chip-select demux — `SPIChipSelectDemux` in `lib/spi_cs_demux.ato` using TI SN74HC138PWR (LCSC C157527), integrated into `ConnectivityGateway` with 3 address GPIOs and 8 active-low CS outputs
- [ ] Add OTA firmware update path for per-channel RP2040 (SWD or USB bootloader via isolator)
- [ ] Evaluate replacing B0505S with higher-power isolated DC-DC — RECOM RB-0505D out of stock on JLCPCB ($15+), not viable currently. Re-evaluate when RP2040 power budget is known.
