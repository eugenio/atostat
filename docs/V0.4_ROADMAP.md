# V0.4 Roadmap

## Build Blockers

- [ ] Resolve B0505S Y-cap false short (upstream `ruben-iteng/dcdc-converters` package fix or custom DC-DC wrapper that omits the anti-EMI capacitor from atopile's electrical DRC)
- [ ] Replace RP2040 placeholder with full `Raspberry_Pi_RP2040` module (needs atopile `has_single_electric_reference_shared` trait fix for isolated power domains)

## Component Quality

- [ ] Replace auto-generated KiCad footprints/symbols with JLCPCB-sourced ones (ADS1256, ISO7741, SN65HVD230)
- [ ] Replace ferrite bead resistor placeholders with proper `FerriteBead` module imports throughout all lib/*.ato files
- [ ] Replace ISO7741 placeholder in `galvanic_isolator.ato` with real `ISO7741_driver` from `lib/iso7741.ato` (blocked by false short detection)

## Architectural

- [ ] Add per-channel SPI chip-select demux for multi-channel RP2040 communication from gateway
- [ ] Add OTA firmware update path for per-channel RP2040 (SWD or USB bootloader via isolator)
- [ ] Evaluate replacing B0505S with higher-power isolated DC-DC (RECOM RB-0505D or Murata MEJ1) for increased current budget with RP2040 added
